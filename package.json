{
  "name": "vanilla-basic",
  "version": "1.0.0",
  "scripts": { 
    "build": "npm install ; node build.mjs"
  },
  "devDependencies": {
    "@iarna/toml": "~2.2",
    "marked": "~4.0",
    "fs-extra": "~10.1"
  }
}
